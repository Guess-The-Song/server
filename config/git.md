# Hint for future me and maybe others

Use `git update-index --skip-worktree <file>` to ignore changes to a file that is already tracked by git. \
E.g. `git update-index --skip-worktree server/config/git.md`